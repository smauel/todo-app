{
  "name": "logger",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "lint": "eslint src --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "pino": "^7.8.0",
    "pino-http": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "@types/pino": "^7.0.5",
    "@types/pino-http": "^5.8.1",
    "tsconfig": "*",
    "typescript": "^4.5.5"
  }
}
